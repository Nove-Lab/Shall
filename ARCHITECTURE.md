# Shall 아키텍처

Shall은 AI 코딩 에이전트를 위한 로컬 spec-driven control plane이다. 사용자 컴퓨터에서
daemon 하나가 돌고, 사람은 localhost 웹 화면에서 스펙을 승인·관찰하며, 에이전트는
프로젝트 안의 YAML 폴더를 읽고 써서 작업한다. SaaS로 설계됐던 이전 버전의 기록은
[`LEGACY-ARCHITECTURE.md`](./LEGACY-ARCHITECTURE.md)에 있다 — 이 문서가 현재의 정본이다.

## 원칙

- **판정에 AI가 없다.** 충족·낡음·게이트·보드는 전부 세고, 비교하고, 시간을 보는
  산술이다. AI는 제안을 만들 뿐이며, 그 AI조차 이 프로그램 밖(접속한 에이전트)에 있다.
- **확정 스펙은 사람만 바꾼다.** 에이전트의 모든 변경은 초안으로 제출되고, 사람이
  승인해야 DB에 앉는다.
- **실행 기록은 지우지 않는다.** 한 번 쓰인 기록은 수정·삭제되지 않는다. 고칠 수 있는
  칸은 증거 판정과 질문 상태, 딱 둘이다.
- **계산 결과는 저장하지 않는다.** 충족률·보드 같은 값은 컬럼이 아니라 매번 그래프에서
  다시 계산한다.
- **같은 그래프는 같은 바이트.** 미러 문서와 base 내보내기는 결정적이라, 그래프가
  같으면 출력도 항상 같다.
- **전체가 하나의 TypeScript 프로그램.** "쓰기 경로가 몇 개인가" 같은 질문에 코드
  전체를 훑어 답할 수 있어야 한다.

## 전체 그림

프로세스는 셋이고, 에이전트는 프로세스가 아니라 **파일 계약**이다.

- **daemon** — DB를 만지는 유일한 프로세스. 웹 화면과 CLI 요청을 받는다.
- **web** — localhost 브라우저 화면. 승인·관찰 전용.
- **cli** — `shall` 명령. daemon을 띄우고, 세션을 열고 제출하는 얇은 클라이언트.
- **에이전트** — 프로젝트의 `.shall/<세션>/` 폴더에서 YAML만 읽고 쓴다. daemon과 직접
  대화하지 않는다 (MCP 없음).

작업 흐름:

1. 에이전트가 CLI로 세션을 연다 → daemon이 현재 스펙 그래프를
   `.shall/<세션>/base/`에 YAML로 내보낸다.
2. 에이전트가 base를 바탕으로 `draft/`에서 자유롭게 수정·추가한다.
3. CLI로 제출하면 daemon이 draft와 base의 차이를 계산하고 규칙 검증을 돌린다.
   base가 낡았으면(그새 그래프가 바뀌었으면) 반려 — 세션이 rebase 후 재제출한다.
4. 검증을 통과한 변경 중 **실행 기록**(작업 로그·증거 등)은 즉시 DB에 앉고,
   **스펙 변경**(요구·설계 등)은 review queue로 가서 사람 승인을 기다린다.
5. 변경이 앉으면 `shall/` 폴더의 사람용 미러 문서가 다시 렌더된다.

## 모듈

### core/graph — 그래프 스키마

스펙 그래프의 문법을 정의한다.

- 노드·엣지 타입 정의 — 타입마다 실제 컬럼을 가진다 (범용 속성 주머니 없음)
- 세 층 배치: 의도(Intent) · 설계(Design) · 실행(Execution)
- 리비전 — 스펙 노드가 개정될 때 전체 스냅샷을 보관
- 연결 시점 버전 — 엣지가 생길 때 상대 노드의 버전을 새겨, 낡음 판정의 기준으로 삼는다

### core/store — 저장

프로젝트별 `.shall/shall.db` SQLite 파일이 정본이다.

- 마이그레이션이 스키마의 유일한 출처
- append-only를 DB 트리거로 강제 — 수정·삭제 시도는 스키마 수준에서 거부
- 고칠 수 있는 두 칸(증거 판정·질문 상태)만 예외로 열고, 판정 변경은 이력 테이블에 남긴다
- daemon만 이 파일을 연다 — 단일 writer
- 백업·이사 = 파일 복사

### core/arith — 판정 산술

그래프에서 값을 계산하는 곳. AI가 닿을 수 없고, 닿을 AI도 없다.

- 충족 계산 — 요구가 채워졌는지를 증거·연결에서 매번 계산 (저장 안 함)
- 낡음 계산과 전파 — 연결 시점 버전과 현재 버전의 비교
- 게이트 판정 — "청구 없는 증거", "증거 없는 완료" 같은 제출 규칙 위반 판별
- 보드 합성 — 작업 보드는 그래프 상태의 함수이지, 저장된 목록이 아니다
- vitals — 그래프 건강 지표 (개수·비율·시각 산술)

### core/serialize — 내보내기

그래프를 파일로 투영하는 결정적 함수들. 같은 그래프면 언제나 같은 바이트.

- 사람용 미러 — `shall/` 폴더의 읽기 좋은 최신 스펙 문서
- 에이전트용 base — 세션 폴더에 넣는, 편집 가능한 YAML 표현
- 순수 함수 규율 — 시간·난수·환경에 의존하지 않고, 정렬 순서는 자체 키로 고정한다

### core/exchange — 세션 중개

에이전트의 작업환경과 DB 사이의 유일한 통로. 세션의 수명과 착지가 한 흐름이라
한 모듈이다 — 에이전트 쓰기가 승인을 우회할 수 없는 이유가 여기 하나로 좁혀진다.

- 세션 개설 — base 내보내기(serialize 호출)와 그래프 버전 스탬프
- 상태 추적: 열림 → 제출됨 → 착지됨 / 낡음. 여러 세션 동시 진행 허용
- 제출 처리 — draft 파싱과 base 대비 diff 계산
- 규칙 검증 — 게이트 위반을 제출 시점에 결정적으로 걸러 리포트로 돌려준다
- 충돌 검사 — base에 새긴 그래프 버전과 현재 DB 버전을 비교, 낡았으면 반려
- rebase — 낡은 세션의 base를 최신으로 갱신
- 착지 — 실행 기록은 즉시, 스펙 변경은 승인 대기(proposed)로

### daemon — 서버

단일 프로세스. 호스트(OS·네트워크·git)를 만지는 코드는 전부 여기에만 있다.

- HTTP 하나로 web 화면(tRPC)과 CLI(세션 API)를 받는다
- git 읽기 — 프로젝트의 커밋 사실을 git log에서 직접 읽어 그래프에 등록
  (증거가 인용할 때 + 주기적으로). 훅 설치 없음, git에 쓰지 않음
- 정적 SPA 서빙, 프로젝트 레지스트리(`~/.shall/`), 설정 파일 관리

### apps/web — 화면

localhost 브라우저 화면. 사람의 승인·관찰 표면.

- Control plane — review queue(스펙 변경 승인·반려) · task board · activity · vitals.
  전부 core/arith 계산 결과의 표시
- Spec plane — 그래프 캔버스(React Flow)와 노드 상세
- Settings — 설정 파일을 그대로 보여주고 고치는 화면

### client/cli — 명령줄

`shall` 명령. 계산도 직렬화도 하지 않는 얇은 클라이언트.

- daemon 기동·재사용, 브라우저 열기 (현행)
- 세션 명령 — 열기·제출·상태·rebase 요청을 daemon에 전달
- 검증 실패를 출력과 세션 폴더의 리포트 파일로 에이전트에게 돌려준다

## 레거시에서 사라진 것

MCP 서버 · webhook 수신 · 외부 cron · 추론 클라이언트와 그 게이트 · 등급/계량 ·
멀티테넌트 격리 · 독립 Verifier (증언 리포트의 노드 타입만 스키마에 예약해 둔다).

## 일찍 얼려야 하는 것

- **미러·base의 파일 형식** — 사용자 repo에 바이트가 남는 순간부터, 형식 변경은
  그들의 git 히스토리를 다시 쓰게 한다
- **YAML 중간표현의 스키마** — 에이전트 워크플로우가 이 형식을 학습하고 하드코딩한다
- **세션 폴더 규약**(`.shall/<세션>/base|draft`)과 CLI 명령 이름
