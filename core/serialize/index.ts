/**
 * core/serialize — graph to files. Empty until there is a graph worth writing.
 *
 * Two projections: the human mirror under `shall/`, and the agent-editable
 * base YAML in a session folder. Both are pure functions of the graph — no
 * clock, no randomness, no environment — so the same graph is always the same
 * bytes.
 */
export {};
