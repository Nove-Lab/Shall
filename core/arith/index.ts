/**
 * core/arith — the judgement arithmetic. Empty until the graph has edges.
 *
 * Everything here is counting, comparing, and reading a clock: coverage,
 * staleness and its propagation, gate verdicts, the task board, vitals. No AI
 * reaches into this module, and none of its results are stored — they are
 * recomputed from the graph on every read.
 */
export {};
